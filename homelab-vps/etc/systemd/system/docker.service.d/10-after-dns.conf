[Unit]
# Containers created before systemd-resolved answers on 127.0.0.53 get the host
# resolv.conf copied verbatim instead of Docker's internal resolver (127.0.0.11),
# leaving them with a dead nameserver until they are recreated.
After=nss-lookup.target
Wants=systemd-resolved.service
